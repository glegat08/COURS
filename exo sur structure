#include <iostream>
#include <string>

struct Contact
{
	std::string first_name;
	std::string last_name;
	int age;
};

struct Friendlist
{
	int contact[]; // ici on veut que contact soit un tableau (comme un carnet)
};

void addContact(Contact &first_name, Contact &last_name, Contact &age/*, Friendlist *contact[]*/)
{
	Contact fname;
	std::cout << "First name : " << std::endl;
	std::cin >> fname.first_name;

	Contact lname;
	std::cout << "Last name : " << std::endl;
	std::cin >> lname.last_name;

	Contact rAge;
	std::cout << "Age : " << std::endl;
	std::cin >> rAge.age;

	std::cout << fname.first_name << " " << lname.last_name << " " << rAge.age << std::endl;

	/*contact[4] = {fname, lname, rAge};*/ // on veut créer une liste avec ces données mais jsp comment faire
}

void removeContact(Friendlist&, Contact&)
{

}

void display(Contact &first_name, Contact &last_name, Contact &age)
{
	int name = first_name.length(); // je veux que name contienne le first_name
	int** lines = nullptr;
	lines = new int* [name];

	for (int i = 0; i < name; ++i) // l'index analyse la taille de name
	{
		lines[i] = new int[name];
	}


	//for (int i = 0; i < first_name.length(); ++i) // comment faire pour afficher la taille de first_name ?
	//	{
	//	std::cout << " " << std::endl;
	//	for (int j = 0; j < columns ; ++j)
	//	}
}


int main()
{
	Contact first_name; Contact last_name; Contact age;

	addContact(first_name, last_name, age);

	return 0;
}
